interface:
  display_name: "Simplify Codebase"
  short_description: "Simplify code or clean AI defensive layers"
  default_prompt: "Use $simplify-codebase. First ask whether to simplify accidental complexity or clean AI-generated defensive layers, then audit without editing files."

policy:
  allow_implicit_invocation: true

